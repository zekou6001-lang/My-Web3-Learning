CSS3 中的多列布局（Multi-column Layout）

1. 核心布局属性
•	column-count：指定布局应分为多少列。例如设置 3，内容会自动平摊到三列中。
•	column-width：指定每一列的理想宽度。浏览器会根据容器的总宽度自动计算最终的列数。
•	columns：这是一个复合属性，可以同时设置列宽和列数。
o	注意：当两者冲突时，浏览器通常会优先满足较小的那个约束。
2. 列间距与分割线
•	column-gap：设置各列之间的间距。即使设置为 0，浏览器也可能保留一个极小的默认边距。
•	column-rule：在列与列之间绘制一条分割线。
o	它的语法与 border 非常相似，由 宽度（width）、样式（style） 和 颜色（color） 组成（如 2px dashed red）。
o	这条线不占用页面的物理空间，只是一个视觉装饰。
3. 跨列处理
•	column-span：决定一个元素是否应该横跨所有列。
o	取值通常为 none（默认，不跨列）或 all（跨越所有列）。
o	常用于报纸风格的标题，让标题横在所有分栏内容之上。
4. 实战应用：瀑布流图片墙
视频演示了多列布局在处理图片时的强大功能。
•	自动排版：只需给图片的父容器设置 column-count，内部的所有图片就会按照从上到下、从左到右的顺序自动填充每一列。
•	防溢出技巧：为了让图片在每一列中完整显示，需要给 img 标签设置 width: 100%。
•	呼吸感：配合 transition 和 box-shadow 等属性，可以轻松实现炫酷的交互式图片墙。
💡 极客总结心法
•	容器属性：绝大多数多列布局属性（如 count, gap, rule）都是设置在父容器上的。
•	内容流动：多列布局最大的特点是“流动性”，它非常适合处理内容长度不确定的富文本，能够有效利用大屏幕的横向空间。
•	瀑布流神器：相比传统的浮动布局，多列布局是实现简单瀑布流效果的最快方案，且不需要复杂的 JavaScript 计算高度。





CSS3 伸缩盒模型（Flexbox） 总结：
1. 伸缩盒模型的背景与称呼
•	发布时间：2009 年由 W3C 提出，是 CSS3 中非常强大的新布局模式。
•	别名：在 MDN 等官方文档中，它被称为 Flexible Box（伸缩盒模型），也常被称为 弹性盒子。这两种说法在技术领域都是标准的。
2. 核心优势
伸缩盒模型极大地简化了复杂的布局需求，主要体现在以下三个方面：
•	方便控制分布方式：可以轻松实现元素在水平或垂直方向上的排列（如一排或一列）。
•	灵活的对齐方式：通过简单的属性，即可实现元素的居中、两端对齐、靠顶/靠底对齐，甚至能处理不同高度元素间的对齐。
•	视觉顺序调整：可以不改变 HTML 代码的先后顺序，仅通过 CSS 改变元素在屏幕上显示的视觉顺序。
3. 浏览器兼容性
•	现状：目前绝大多数现代浏览器（Chrome, Firefox, Safari 等）都已完全支持。
•	痛点：它对 IE 浏览器 并不友好，尤其是低版本 IE（IE6-9 不支持，IE10-11 仅部分支持且存在 Bug）。
4. 传统布局 vs. Flex 布局
•	传统布局：主要基于 盒子模型，依赖 display、position 和 float（浮动）属性。这种方式在处理复杂排列和居中时往往需要大量代码且容易出错。
•	Flex 布局：更具弹性，目前在 移动端（手机端）应用最为广泛，因为移动端屏幕尺寸多样，Flex 的弹性特征能完美适配。虽然 PC 端应用也越来越多，但受限于老旧 IE 浏览器的兼容性要求，普及速度略慢于移动端。
5. 总结
伸缩盒模型（Flex 布局）是为了解决传统布局（如浮动、定位）的局限性而生的。它通过一套全新的属性，让开发者能够以更直观、更简洁的方式控制页面元素的尺寸、位置和排列顺序。



CSS3 伸缩盒模型（Flexbox）中的**容器（Container）与项目（Item）**的概念及基本使用

1. 核心概念
•	伸缩容器（Flex Container）：任何开启了 display: flex 或 display: inline-flex 的元素。
•	伸缩项目（Flex Item）：伸缩容器的所有直接子元素（仅限一代，孙子辈及以后不是项目）。
2. 开启方式
•	display: flex：这是最常用的方式，容器会作为块级元素呈现。
•	display: inline-flex：较少使用，容器会作为行内块呈现（类似于 inline-block），多个容器可以并排显示。
3. “项目”的特性：块状化
一旦元素成为伸缩项目，它就会自动**“块状化”**。
•	无论元素原本是行内元素（如 <span>）、块级元素还是行内块元素，成为项目后都可以直接设置宽度和高度。
•	项目默认会按照伸缩盒的规则排列，例如多个 div 成为项目后会自动横向排列，且不会像浮动那样脱离文档流。
4. 重点提示
•	非递归性：只有容器的“亲生儿子”才是项目。如果想让孙子辈也实现伸缩布局，需要给其父元素（即目前的某个项目）也开启 flex。
•	开发者工具使用：视频演示了如何利用浏览器的“计算样式”（Computed）面板查看元素最终的布局属性，这在调试 rem 或百分比单位时非常有用。
5. 布局优势（对比浮动）
•	不脱离文档流：项目依然在文档流内，父元素的高度能被子元素正常撑开，不需要手动清除浮动。
•	计算简单：配合 box-sizing: border-box，可以更精确、直观地控制每个盒子的最终尺寸。



CSS3 伸缩盒模型（Flexbox）中的侧轴对齐方式，并根据容器内项目的行数分为两种情况进行讨论



1. 单行情况下的对齐 (align-items)
当伸缩容器内只有一行项目时，使用 align-items 属性来控制它们在侧轴（通常是垂直方向）上的对齐：
•	flex-start：侧轴的起点对齐（默认通常是顶部）。
•	flex-end：侧轴的终点对齐（底部）。
•	center：侧轴的中点对齐（居中）。
•	baseline：项目内第一行文字的基线对齐。
•	stretch（默认值）：如果项目未设置高度，它将拉伸以填满整个容器的侧轴高度。
2. 多行情况下的对齐 (align-content)
当项目由于换行（flex-wrap: wrap）产生了多行时，使用 align-content 来控制行与行之间的分配和对齐：
•	flex-start：所有行紧贴侧轴起点。
•	flex-end：所有行紧贴侧轴终点。
•	center：所有行在侧轴居中。
•	space-around：每行两侧距离相等，行与行之间的间距是边缘间距的两倍。
•	space-between：首尾行紧贴边缘，中间行平分剩余空间。
•	space-evenly：行与行之间、行与边缘之间的距离全部绝对相等。
•	stretch（默认值）：如果项目未设置高度，各行将平分并拉伸以填满容器侧轴。
3. 关键区别与提示
•	属性适用对象：单行看 align-items，多行看 align-content。
•	拉伸的前提：stretch 属性生效的前提是项目没有显式设置高度。如果设置了高度，拉伸将失效，表现通常类似于 flex-start。
•	侧轴高度：在多行布局中，每一行所占的高度是由该行中最高的那一个项目决定的。

