JavaScript 中对象（Object）的属性名和属性值的基础知识 
1. 属性名（Property Name）
属性名是对象中存储数据的标识符。
•	命名规则： 对象的属性名不强制遵守标识符的规范（即可以包含特殊字符，甚至可以使用关键字作为属性名），但为了规范，建议还是尽量按照标识符规则来命名。
•	读取与设置方式：
o	点操作符（.）： 最常用的方式。例如 obj.name = "孙悟空";。但是，如果属性名不符合标识符规范（例如以数字开头），则不能使用点操作符。
o	中括号操作符（[]）： 当属性名比较特殊或者需要动态变化时使用。语法：对象["属性名"] = 属性值;。
	灵活性： 中括号内可以传递一个变量。变量的值是什么，就会读取对应的属性名。这比点操作符更加灵活。
2. 属性值（Property Value）
属性值是对象属性中实际存储的数据。
•	数据类型： JS 对象的属性值可以是任意数据类型。
o	可以是基本数据类型：String、Number、Boolean、Null、Undefined。
o	也可以是一个对象：属性值指向另一个对象，从而实现对象的嵌套。例如：obj.test = new Object();。
3. in 运算符
in 运算符用于检查一个对象中是否含有指定的属性。
•	语法： "属性名" in 对象
•	返回值： 如果对象中含有该属性，则返回 true；如果不含有，则返回 false。
•	注意： 检查的属性名必须用引号括起来（除非是变量）。
总结
•	点操作符简单直观，但有命名限制。
•	中括号操作符功能更强大，支持特殊属性名和变量动态访问。
•	属性值极其灵活，可以承载任何 JS 支持的数据。
•	in 运算符是判断属性存在性的便捷工具。




JavaScript 中基本数据类型与引用数据类型的区别，主要围绕内存结构、变量赋值和比较运算展开。
1. 数据类型的分类
JavaScript 的数据类型分为两大类：
•	基本数据类型（Primitive Data Types）： String、Number、Boolean、Null、Undefined。
•	引用数据类型（Reference Data Types）： Object（对象，包括数组、函数等）。
2. 内存结构的存储方式
这是理解两者区别的核心：
•	栈内存（Stack）： 所有的变量名都存储在栈内存中。基本数据类型的值也直接存储在栈内存中。由于基本类型占据空间小、大小固定，由系统自动分配和释放，查询速度快。
•	堆内存（Heap）： 引用数据类型（对象）存储在堆内存中。堆内存用于存储占据空间大、大小不固定的数据。
•	联系： 当创建一个对象时，会在堆内存中开辟一个空间保存该对象，并在栈内存中保存该对象的内存地址（引用）。变量实际上保存的是这个地址，而不是对象本身。
3. 变量赋值的区别
•	基本类型赋值： 当把一个基本类型的变量赋值给另一个变量时，会在栈内存中创建一个值的副本。此后这两个变量完全独立，修改其中一个不会影响另一个。
•	引用类型赋值： 当把一个对象变量赋值给另一个变量时，复制的是内存地址。这意味着两个变量指向堆内存中的同一个对象。通过其中一个变量修改对象属性，另一个变量也会感知到变化。
4. 比较运算符的区别
•	基本类型比较： 比较的是值是否相等。
•	引用类型比较： 比较的是内存地址是否相同。
o	即使两个对象拥有完全相同的属性和值，只要它们是分别创建的（位于不同的堆内存地址），它们也不相等（obj1 == obj2 结果为 false）。
5. 特殊情况：断开引用
•	如果将一个指向对象的变量设置为 null（如 obj = null;），这只是在栈内存中清空了该变量保存的地址，断开了变量与堆中对象的联系。
•	如果还有其他变量指向该对象，该对象在堆内存中依然存在，不会被销毁。
总结
•	基本类型： 值与值之间独立，在栈中直接存储值。
•	引用类型： 变量存的是地址，多个变量可操作同一个堆对象。



JavaScript 中创建对象的另一种重要方式——对象字面量（Object Literal）。
1. 什么是对象字面量
对象字面量是一种简洁的创建对象的方法，使用大括号 {} 直接定义对象。
•	对比传统方式： * 传统方式：var obj = new Object();（使用构造函数）。
o	字面量方式：var obj = {};。
•	本质： 两种方式在底层运行逻辑上是一致的，但字面量方式更加简便、直观，是开发中最常用的方式。
2. 创建时直接指定属性
使用对象字面量，可以在创建对象的同时为其指定属性，而不需要像传统方式那样一行行去添加。
•	语法结构： {属性名: 属性值, 属性名: 属性值}
•	关键规则：
o	冒号（:）： 连接属性名和属性值。
o	逗号（,）： 分隔多个名值对。
o	引号： 属性名一般不加引号（除非包含特殊字符或不符合标识符规范，则必须加引号）。
3. 对象嵌套
属性值可以是任何数据类型，包括另一个对象字面量。
•	示例：var obj = { name: "孙悟空", test: { a: 1, b: 2 } };
•	这允许开发者构建复杂的多层级数据结构。
4. 代码风格建议
•	多行书写： 当对象属性较多时，建议将每个名值对放在独立的一行，并使用缩进，这样结构会非常清晰（类似于 CSS 的书写格式）。
•	末尾逗号： 最后一个属性值后面不要写逗号。虽然现代浏览器可以识别，但在一些老旧浏览器中可能会导致报错。
总结
•	对象字面量是 JS 创建对象的“快捷方式”。
•	核心格式是：大括号、冒号连接、逗号分隔。
•	它极大地提高了代码的阅读性和编写效率，是处理 JSON 数据和配置项时的标准写法。



JavaScript 中**函数（Function）**的基础概念及其初识。
1. 函数的本质
•	函数也是一个对象：在 JS 中，除了基本数据类型，几乎一切皆对象。函数是具有特殊功能的对象，专门用来封装可执行的代码。
•	功能封装：函数可以将一段完成特定功能的代码块保存起来，在需要时统一执行。
2. 函数的作用（优点）
•	封装代码：将复杂的逻辑隐藏在函数内部。
•	代码复用：一次编写，多次调用。如果一段代码需要在不同地方运行，只需将其封装成函数，而不必重复编写。
•	结构清晰：使程序更加模块化，便于维护。
3. 创建函数的一种方式：构造函数（了解）
视频中演示了使用构造函数创建一个函数对象的方法：
•	语法：var 函数名 = new Function("代码字符串");
•	特点：代码以字符串的形式传递。虽然这种方式能更直观地体现“函数是对象”，但在实际开发中极少使用，因为其编写不便且性能较差。
4. 函数的调用
•	语法：函数对象()。
•	运行机制：当调用函数时，封装在函数体内的代码会按照顺序执行。如果函数没有被调用，其中的代码永远不会执行。
5. 函数作为对象的特性
•	添加属性：既然函数是对象，它就可以像普通对象一样添加属性和方法。
o	例如：fun.name = "MyFunction";。
总结
•	函数是 JS 中极其重要的工具，它的核心价值在于代码的封装与重用。
•	记住：调用函数才会执行其中的代码；不调用则代码仅作为定义存储在内存中。


1. 什么是函数？
函数是一个专门用来封装可执行代码的对象。
•	封装的意义：将一段逻辑复杂的代码打包，使其成为一个整体。
•	主要优点：
o	代码复用：一次编写，随处调用。
o	逻辑清晰：使代码模块化，便于后期的维护和阅读。
2. 创建函数的三种方式（语法）
在 JavaScript 中，主要有以下三种方式来创建函数对象：
方式一：使用构造函数（Constructor）
这种方式最能体现“函数是对象”这一本质。
•	语法：var 变量名 = new Function("执行代码字符串");
•	示例：var fun = new Function("console.log('这是我的第一个函数');");
•	特点：将执行的代码以字符串的形式传入。
•	缺点：书写非常麻烦，且不符合正常的编程逻辑，在实际开发中几乎不使用。
方式二：函数声明（Function Declaration）—— 核心重点
这是最常用、最标准的创建方式。
•	语法：
JavaScript
function 函数名([形参1, 形参2...]) {
    语句...
}
•	解析：
o	function：关键字。
o	函数名：遵循标识符命名规范。
o	()：括号内可以定义参数。
o	{}：大括号内是函数体（要执行的代码）。
•	优点：结构清晰，支持函数声明提升（在定义之前也可以调用，虽然不建议这样做）。
方式三：函数表达式（Function Expression）
这种方式本质上是创建一个匿名函数，并将其赋值给一个变量。
•	语法：
JavaScript
var 变量名 = function([形参1, 形参2...]) {
    语句...
};
•	解析：
o	先定义一个变量。
o	使用 function(){} 创建一个没有名字的函数（匿名函数）。
o	将匿名函数对象赋值给该变量。
•	注意：结尾通常建议加上分号 ;。
3. 函数的调用
无论通过哪种方式创建，函数体内的代码都不会自动执行。必须通过调用来触发。
•	语法：函数对象() 或 变量名()
•	示例：fun();
•	运行逻辑：调用函数时，解析器会按照顺序执行函数封装体内的每一行代码。
总结与对比
1.	构造函数方式：属于底层理解范畴，开发中弃用。
2.	函数声明：最直接的定义方式，通常用于定义全局或功能模块。
3.	函数表达式：将函数视为一个“变量值”，在回调函数或闭包中非常灵活。






JavaScript 函数中**参数（Parameters）**的使用，这是让函数变得灵活和通用的关键。
1. 形参（形式参数）
•	定义： 在定义函数时，在函数名后的括号 () 中指定的变量名。
•	作用： 它们相当于在函数内部声明了变量，但没有赋值。它们作为占位符，等待函数被调用时接收具体的数据。
•	语法： function 函数名(形参1, 形参2, ...){ ... }
2. 实参（实际参数）
•	定义： 在调用函数时，在括号 () 中传入的具体值。
•	作用： 实参会按照顺序赋值给对应的形参。
•	语法： 函数名(实参1, 实参2, ...);
3. 参数的传递机制
•	一一对应： 第一个实参给第一个形参，第二个给第二个，以此类推。
•	数量不匹配的情况：
o	实参多于形参： 多出来的实参不会被赋值给形参，也不会报错（它们被存储在 arguments 对象中，但基础讲解中通常先强调形参接收不到）。
o	实参少于形参： 没有对应实参的形参将会是 undefined。
4. 参数的数据类型
•	无限制： JavaScript 是一门弱类型语言，函数的实参可以是任何数据类型（数字、字符串、布尔值、对象、甚至另一个函数）。
•	不检查类型： 解析器不会检查实参的类型。如果函数内部逻辑要求数字，但你传了字符串，可能会导致逻辑错误（如变成字符串拼接），因此在开发中需要注意数据类型的把控。
总结
•	形参是声明，实参是赋值。
•	通过参数，同一个函数可以处理不同的数据，实现真正的代码复用。
•	调用时必须注意实参的顺序，以确保函数逻辑正确执行。




JavaScript 函数中**返回值（Return Value）**的概念及其用法。
1. 什么是返回值？
返回值是函数执行完成后，向调用者“返回”的结果。
•	为什么要返回值？ 通常我们调用函数是为了得到一个计算结果，而不是仅仅让函数执行一个操作（如 alert 弹窗）。
•	生活中的例子： 比如你去称体重。你站上秤，秤计算出你的体重（函数执行），然后把数值告诉你（返回值）。你拿到这个数值后，可以选择记录下来、告诉别人，或者什么都不做。这比秤自动大声报出你的体重（类似于 alert）更加灵活且隐私。
2. return 关键字
在函数体中，使用 return 关键字来定义返回值。
•	语法： return 返回值;
•	示例：
JavaScript
function sum(a, b, c) {
    var d = a + b + c;
    return d; // 将计算结果 d 返回
}
•	获取返回值： 调用函数时，可以使用一个变量来接收这个返回值。
JavaScript
var result = sum(1, 2, 3); // result 的值就是 6
3. return 的“停止”作用
return 关键字不仅能返回值，它还会立即终止当前函数的执行。
•	执行顺序： 当函数执行到 return 语句时，它会交出结果并直接退出。
•	死代码： 位于 return 之后的任何代码都不会被执行。
JavaScript
function test() {
    return 1;
    console.log("这行代码永远不会运行"); // 这就是死代码
}
4. 默认返回值
•	未定义 return： 如果函数内部没有写 return 语句，函数执行完毕后会默认返回 undefined。
•	空 return： 如果只写了 return; 而后面没有跟任何值，其效果也等同于返回 undefined。
5. 返回值的多样性
return 后面可以跟任何类型的值：
•	基本数据类型： 数字（Number）、字符串（String）、布尔值（Boolean）等。
•	引用数据类型： 可以返回一个对象（Object），甚至可以返回另一个函数（Function）。
总结
•	返回值让函数变得“有来有往”：你给它参数（输入），它还你结果（输出）。
•	使用 return 可以让代码更加灵活，因为你可以在函数外部决定如何处理这个结果（打印、保存或参与下一步计算）。
•	记住 return 是函数的终点，一旦触发，函数立即结束。



JavaScript 函数参数的灵活性，核心知识点在于：函数的实参可以是任何类型的值（包括基本数据类型、对象、甚至另一个函数）。
1. 基础实战：逻辑封装与代码优化
视频首先通过两个简单的例子演示了如何利用参数和返回值封装功能：
•	判断偶数练习： * 定义一个函数，接收一个数字作为参数。
o	优化技巧： 讲解了如何将复杂的 if...else 逻辑简化。与其写 if(num % 2 == 0){ return true; }，不如直接 return num % 2 == 0;。因为比较运算本身就会返回一个布尔值，这样代码更优雅、高效。
•	圆面积计算： * 参数为半径 r，返回计算结果 $\pi \times r^2$。
2. 进阶实战：实参可以是“对象”
这是本课的重点之一。当一个函数需要接收大量参数（例如：姓名、年龄、性别、住址等）时，直接传参会面临两个问题：
1.	参数过多，导致函数调用时难以阅读。
2.	顺序要求严格，一旦传错位置，逻辑就会出错。
•	解决方案： 将所有参数封装进一个对象中，然后只将该对象作为实参传递。
•	优点： * 不需要记忆参数顺序，只需确保对象的属性名正确。
o	代码结构更清晰，易于维护。
3. 高级实战：实参可以是“函数”
由于在 JavaScript 中函数本质上也是一种对象，因此函数可以作为参数传递给另一个函数。
•	核心区别（易错点）：
o	fun：代表函数对象本身。将其作为参数传递，意味着把整个功能交给了另一个函数。
o	fun()：代表调用函数。将其作为参数传递，实际上是把该函数运行后的返回值传了过去。
•	应用场景： 这是实现“回调函数”的基础。你可以定义一个通用函数，然后通过传入不同的功能函数来改变它的行为。
总结
•	参数多样性： 实参不限于数字或字符串，它可以是 Object 或 Function。
•	对象传参： 用于解决参数过多、顺序混乱的问题。
•	函数传参： 展示了 JS 作为函数式编程语言的强大灵活性。




JavaScript 函数中返回值的不同类型，以及 return 与其他流程控制语句（break、continue）的区别。
1. return、break 与 continue 的区别
这三个关键字都用于控制程序的执行流，但作用范围不同：
•	break：用于退出当前的循环（如 for、while）或 switch 语句。
•	continue：用于跳过当前次循环，直接开始下一次循环。
•	return：用于结束整个函数。
o	无论 return 处在函数的什么位置（即使在多层循环内部），一旦执行，整个函数立即停止并返回结果，函数后面的所有代码都不会再执行。
________________________________________
2. 返回值的多样性
在 JavaScript 中，函数可以返回任何类型的值。
① 返回基本数据类型
可以返回数字、字符串、布尔值、null 或 undefined。
•	如果只写 return; 或不写 return，函数默认返回 undefined。
② 返回对象（Object）
函数可以返回一个对象字面量或通过构造函数创建的对象。
•	语法示例：
JavaScript
function fun() {
    return { name: "孙悟空" };
}
•	这在实际开发中非常有用，比如一个函数需要同时返回多个相关的数据。
③ 返回函数（Function）—— 进阶重点
由于函数在 JS 中也是一种对象，所以一个函数可以把另一个函数作为结果返回。
•	代码结构：
JavaScript
function fun() {
    function inner() {
        alert("我是内部函数");
    }
    return inner; // 注意：返回的是函数对象本身，不要加括号
}
•	如何调用返回的函数？
1.	分步调用：
JavaScript
var a = fun(); // 此时 a 就是 inner 函数
a();           // 执行 inner
2.	直接调用：
JavaScript
fun()();       // 第一对括号执行 fun，第二对括号执行它返回的内部函数
________________________________________
3. 重要细节：返回函数 vs 执行并返回
这是初学者最容易混淆的地方：
•	return inner;：返回的是“机器”本身（函数对象）。你可以把它交给变量，等以后再用。
•	return inner();：返回的是“机器运行后的结果”。它会先运行 inner 函数，然后把 inner 的返回值再返出去。
总结
•	return 是最彻底的停止：它能从函数内部任何地方直接“跳”出来。
•	一切皆可返回：从简单的数字到复杂的对象，甚至是功能完整的另一个函数。
•	函数嵌套返回：这是 JS 高级特性（如闭包、高阶函数）的基础。



JavaScript 中的立即执行函数（IIFE, Immediately Invoked Function Expression）。这是一种非常实用的编程技巧，尤其在处理作用域和模块化时。
1. 什么是立即执行函数？
立即执行函数，顾名思义，就是在函数定义完之后立即被调用的函数。
•	特点： 这种函数通常没有名称（匿名函数），且在执行一次后就无法再次被调用，因为没有变量保存它的引用。
•	主要用途： * 封装代码，避免全局变量污染。
o	只需要执行一次的代码逻辑（如初始化操作）。
2. 语法演变与实现（重点）
如果你直接写一个匿名函数，JavaScript 解析器会报错。
错误示例：
JavaScript
function(){
    alert("我是一个匿名函数");
}
•	报错原因： 解析器会将 function 关键字开头的语句当作“函数声明”，而函数声明必须有名字。此外，它会误把后面的 {} 当成代码块，导致语法混乱。
正确步骤：
1.	用括号包裹函数： 将整个匿名函数用一对圆括号 () 包裹起来。
o	(function(){ ... })
o	原理： 括号在 JS 中是“分组运算符”。被括号包裹后，解析器会将其识别为一个函数表达式（即一个函数对象），而不是函数声明。
2.	立即调用： 在包裹函数的括号后面再加一对括号 ()。
o	(function(){ ... })();
o	原理： 第二对括号就是调用运算符，它会立即执行前面那个括号里代表的函数对象。
________________________________________
3. 如何传递参数
立即执行函数同样可以像普通函数一样接收参数：
•	定义形参： 在第一个括号的 function 后面定义形参。
•	传递实参： 在最后一对调用括号中传入具体数值。
代码示例：
JavaScript
(function(a, b) {
    console.log("a = " + a);
    console.log("b = " + b);
})(123, 456);
在这个例子中，函数被定义后立即运行，并打印出 a = 123 和 b = 456。
________________________________________
4. 总结
•	立即执行函数的核心在于利用括号将“函数声明”转化为“函数表达式”，并随即触发调用。
•	一次性执行： 它是“即插即用”的，执行完就释放，不会在内存中留下变量名。
•	作用域保护： 在 IIFE 内部声明的变量都是局部变量，不会干扰到外部的全局环境，这在早期的 JS 开发中是防止命名冲突的重要手段。



JavaScript 中**方法（Method）**的概念及其与普通函数的区别。 
1. 什么是方法？
在 JavaScript 中，函数也是一种对象。我们可以将一个函数作为一个对象的属性保存。
•	定义： 如果一个函数作为一个对象的属性保存，那么我们称这个函数是这个对象的方法。
•	本质： 方法本质上就是一个函数，只是它被“绑定”到了某个特定的对象上。
2. 方法的语法
可以通过以下两种主要方式为对象添加方法：
① 使用点操作符（外挂式）
先创建一个对象，然后给它添加一个属性，并将函数赋值给该属性。
JavaScript
var obj = new Object();
obj.name = "孙悟空";
// 给对象添加方法
obj.sayName = function() {
    console.log(obj.name);
};
② 使用对象字面量（内嵌式）
在定义对象的同时定义方法，这是最常用的方式。
JavaScript
var obj2 = {
    name: "猪八戒",
    age: 18,
    // 在对象内部定义方法
    sayName: function() {
        console.log("我的名字是：" + this.name);
    }
};
________________________________________
3. 方法的调用
调用方法与调用普通函数的语法非常相似，但必须通过对象来引导。
•	语法： 对象.方法名()
•	示例： obj.sayName(); 或 obj2.sayName();
•	运行逻辑： 调用方法时，解析器会找到对象中对应的属性值（那个函数），并执行其中的代码。
________________________________________
4. 方法与函数的区别（重点）
这是初学者最容易产生疑惑的地方。其实它们在底层是一样的，区别仅在于调用方式和称呼习惯：
特性	函数 (Function)	方法 (Method)
所属关系	独立存在或属于全局（window）	明确属于某个自定义对象
调用语法	fun()	obj.fun()
称呼	独立调用的叫“函数”	通过对象调用的叫“方法”
形象比喻：
•	函数： 像是一个路人，谁都可以叫他去做事。
•	方法： 像是某个人的私人助理，你必须先找到那个人，才能让助理做事。
________________________________________
5. 总结
•	方法就是属性值为函数的属性。
•	引入方法的目的是为了将数据（属性名/值）与行为（函数逻辑）封装在一起，使代码更符合面向对象的逻辑。
•	在调用时，带括号 obj.sayName() 是执行方法；不带括号 obj.sayName 是获取该方法对象本身。




JavaScript 中的**全局作用域（Global Scope）**及其核心特性。
1. 作用域的基本概念
•	定义： 作用域指一个变量或函数在程序中能够被访问的有效范围。
•	JS 中的分类：
1.	全局作用域：编写在 <script> 标签之内的代码。
2.	函数作用域：定义在函数内部的代码（后续视频会详述）。
2. 全局作用域的生命周期与 window 对象
•	生命周期： 全局作用域在页面打开时创建，在页面关闭时销毁。
•	全局对象 window： * 在全局作用域中，JS 提供了一个默认的全局对象 window，它代表了整个浏览器窗口。
o	变量关联： 我们在全局作用域中创建的变量，都会作为 window 对象的属性保存。例如：声明 var a = 10;，可以通过 window.a 访问。
o	函数关联： 我们在全局作用域中创建的函数，都会作为 window 对象的方法保存。例如：定义 function fun(){...}，可以通过 window.fun() 调用。
•	可见性： 全局作用域中的变量和函数具有“全局性”，在页面的任意位置都可以访问到。
________________________________________
3. 变量的声明提前（Variable Hoisting）
这是一个非常重要的 JS 特性，容易导致面试中的坑：
•	规则： 使用 var 关键字声明的变量，会在所有的代码执行之前被声明（但不会赋值）。
•	表现： 如果在变量声明语句之前访问该变量，不会报错，但会得到 undefined。
•	注意： 如果声明变量时不使用 var 关键字（例如直接写 a = 123;），则该变量不会具有“声明提前”的特性。
________________________________________
4. 函数的声明提前（Function Hoisting）
函数的提前行为取决于你是如何定义函数的：
① 函数声明形式
•	语法： function fun(){ ... }
•	特性： 使用这种形式创建的函数，会在所有代码执行之前就被创建完成。
•	结果： 你可以在函数定义语句之前就成功调用该函数。
② 函数表达式形式
•	语法： var fun = function(){ ... };
•	特性： 这种形式本质上是变量声明。根据变量提前规则，变量 fun 会被提前声明，但此时它的值是 undefined 。
•	结果： 如果在定义之前调用它，会报错（因为你正在尝试调用一个 undefined），它不能在声明前被调用。
总结
•	全局作用域是代码的顶层容器，其核心是 window 对象。
•	变量声明提前仅提前“名”而不提前“值”。
•	函数声明整体提前，而函数表达式遵循变量提前规则。理解这些规则能帮助你更好地掌握 JS 代码的执行顺序。




JavaScript 中的函数作用域（Function Scope）。它是作用域体系中非常重要的一部分，决定了变量的可见性和生命周期。
 
1. 函数作用域的基本定义
•	什么是函数作用域：调用函数时创建的作用域。函数执行完毕后，该作用域会被销毁。
•	独立性：每调用一次函数，就会创建一个新的、独立的函数作用域。
•	局部变量：在函数作用域中定义的变量被称为“局部变量”。
2. 变量的查找规则（作用域链）
在函数作用域内操作一个变量时，解析器会遵循以下寻找逻辑：
1.	就近原则：首先在当前作用域中寻找。
2.	向外扩展：如果当前作用域没有找到，则去上一级作用域中寻找。
3.	最终归宿：直到找到全局作用域。如果全局作用域中依然没有找到，则会报错：ReferenceError。
3. 内外部的访问限制（重点）
•	里看外：在函数内部可以访问到全局变量。
•	外看里：在全局作用域（或外部函数）中，无法访问到函数内部定义的局部变量。这起到了很好的代码封装和保护作用，防止变量污染全局。
4. 函数内部的声明提前（Hoisting）
函数作用域内的变量和函数也遵循提前规则，其作用范围仅限于该函数内部：
•	变量声明提前：使用 var 关键字声明的局部变量，会在函数中所有代码执行前被声明（但不会赋值）。如果在声明前访问，结果是 undefined。
•	函数声明提前：在函数内部定义的函数，如果是以 function 名字(){} 这种声明形式定义的，它也会在函数内部代码执行前被创建好，可以在定义之前被调用。
5. 全局变量的隐式创建（避坑指南）
•	显式声明：在函数内部使用 var a = 10; 声明的是局部变量。
•	隐式创建：如果在函数内部没有使用 var 直接给一个变量赋值（如 a = 10;），JS 会将其视为全局变量（即 window.a）。这是一个常见的错误来源，建议始终明确声明变量。
6. 形参与局部变量
•	函数的形参（Parameters）本质上就是在函数内部声明的局部变量。
•	这意味着在函数内部通过形参接收的数据，其生命周期也仅限于该函数执行期间。
总结
•	作用域是代码的“地盘”：函数作用域是函数私有的地盘。
•	双向隔离：外部不能进（访问内部），内部可以出（访问外部，但受限于遮蔽效应）。
•	理解嵌套：当函数发生嵌套时，就会形成多层作用域。查找变量总是从内向外“爬梯子”。
JavaScript 的**调试（Debug）**基础，主要教大家如何利用浏览器的开发者工具来观察代码的执行流程和内存状态。
 
1. 为什么要调试（Debug）？
在编写复杂代码时，仅凭眼睛看很难发现逻辑错误。通过调试，我们可以：
•	控制执行节奏：让代码一行一行运行。
•	实时查看变量：观察变量在每一行运行完后的具体数值。
•	理解底层逻辑：直观地看到“声明提前”等机制在内存中是如何表现的。
2. 如何开启调试工具
•	快捷键：在绝大多数浏览器（Chrome, Firefox, IE/Edge）中，按下 F12 键即可打开开发者工具。
•	菜单面板：
o	Chrome：选择 Sources（源代码）面板。
o	Firefox：选择 Debugger（调试器）面板。
o	IE：选择 调试程序 面板。
3. 断点（Breakpoints）
断点是调试的核心，它告诉浏览器：“运行到这一行时先停下来，等我的指令。”
•	设置方法：在代码显示的行号位置点一下。通常会出现一个蓝条或红点，表示断点设置成功。
•	生效条件：设置断点后，通常需要刷新页面，代码运行到该处时就会自动暂停。
4. 变量监控与作用域（Watch & Scope）
当代码在断点处停下时，我们可以查看当前的“案发现场”：
•	Watch（监视）窗口：你可以手动添加变量名（如变量 a），工具会实时显示它的当前值。
•	Scope（作用域）列表：
o	可以看到 Global（全局作用域）。展开 Window 对象，你能找到所有声明提前的变量和函数。
o	可以看到 Local（局部作用域）。如果代码运行在函数内部，这里会显示该函数的局部变量。
5. 控制代码执行
调试工具上方有一组控制按钮（或使用快捷键）：
•	分步执行 / 步过（Step over）：通常是 F10 或一个弯曲的箭头图标。每点一次，代码就向下走一行。这是最常用的功能。
•	恢复运行（Resume）：通常是 F8 或一个“播放”图标。点击后，代码会一直运行到下一个断点或结束。
6. 实战应用：观察“声明提前”
视频通过调试演示了一个经典案例：
•	场景：在 var d = 35; 之前使用 alert(d);。
•	调试表现：
1.	代码还没开始跑，你在全局作用域中就能发现 d 已经存在了，但值是 undefined。
2.	当执行到 alert 时，弹出的就是 undefined。
3.	当执行过赋值语句后，监控窗口里的 d 瞬间变成了 35。
•	结论：这用实验证明了变量确实会“提前声明”但不会“提前赋值”。
7. 总结
•	调试不是目的，而是手段：它是用来验证你的理论（如作用域、提升机制）是否正确的。
•	跨浏览器通用性：虽然不同浏览器的界面略有差异，但“设置断点、分步执行、监视变量”这三招是通用的。



JavaScript 中，this 是一个非常重要但也容易混淆的概念。这个视频详细剖析了 this 的本质及其指向规则。
1. this 的本质
•	隐含参数：解析器（浏览器）在每次调用函数时，都会向函数内部传递一个隐含的参数，这个参数就是 this。
•	对象引用：this 指向的是一个对象，这个对象我们称为“函数执行的上下文对象”。
•	动态性：this 指向的对象并不是固定的，它会根据函数的调用方式的不同而指向不同的对象。
________________________________________
2. 核心准则（最重要的一点）
this 的指向与函数的定义方式无关，只与函数的“调用方式”有关。 即使是同一个函数，用不同的方式调用，内部的 this 也会指向不同的地方。
________________________________________
3. this 的指向规则
根据视频中的演示，主要分为以下两种基本情况：
情况一：以“函数形式”调用
当一个函数被直接调用（即不带对象前缀）时，this 永远指向 window 对象（全局对象）。
•	代码示例：
JavaScript
function fun() {
    console.log(this);
}
fun(); // 打印结果：[object Window]
•	原理解析：在全局作用域中调用函数，本质上是执行了 window.fun()，所以 this 指向全局。
情况二：以“方法形式”调用
当一个函数被赋值给某个对象的属性，并通过对象来调用（即 对象.方法名()）时，this 指向调用该方法的那个对象。
•	代码示例：
JavaScript
var obj = {
    name: "孙悟空",
    sayName: function() {
        console.log(this.name);
    }
};
obj.sayName(); // 打印结果："孙悟空"（这里的 this 指向 obj）
•	原理解析：谁调用的方法，this 就指向谁。
________________________________________
4. this 的实际作用
使用 this 的最大好处是解耦。
•	如果在函数内部写死对象名（如 console.log(obj.name)），那么这个函数就只能为 obj 服务。
•	如果使用 this.name，函数就变得通用了。无论哪个对象调用它，它都能正确获取该对象的 name 属性，从而提高了代码的灵活性和复用性。
总结
判断 this 指向的“三步走”：
1.	找到函数调用的那行代码。
2.	看函数名括号 () 前面有没有点 .。
3.	有点：点前面是谁，this 就是谁；没点：this 就是 window。


JavaScript 中一种重要的设计模式——工厂方法（Factory Method），用于解决批量创建对象时代码冗余的问题。
1. 为什么要使用工厂方法？
在之前的学习中，我们通过对象字面量（{}）或 new Object() 来创建对象。
•	问题： 如果需要创建大量相似的对象（例如：100个“人”对象，都有姓名、年龄、性别），手动编写每一行代码会导致大量的重复劳动（Copy & Paste）。
•	解决方案： 将创建对象的公共逻辑提取出来，封装到一个函数中。这个函数就像一个“工厂”，你给它原材料（参数），它就吐出一个成品（对象）。
2. 工厂方法的实现步骤
通过创建一个专门负责“造对象”的函数来实现。
代码结构示例：
JavaScript
function createPerson(name, age, gender) {
    // 1. 创建一个新的对象
    var obj = new Object();
    
    // 2. 向对象中添加属性
    obj.name = name;
    obj.age = age;
    obj.gender = gender;
    
    // 3. 向对象中添加方法
    obj.sayName = function() {
        alert(this.name);
    };
    
    // 4. 将对象返回
    return obj;
}
3. 如何调用工厂方法
调用工厂方法非常简单，只需传入不同的参数，即可快速获得独立的对象实例：
JavaScript
var obj2 = createPerson("孙悟空", 18, "男");
var obj3 = createPerson("猪八戒", 28, "男");
var obj4 = createPerson("白骨精", 16, "女");

obj2.sayName(); // 弹出：孙悟空
4. 关键点解析
•	参数传递： 通过形参接收数据，赋值给新对象的属性，使得每个造出来的对象数据都是个性化的。
•	this 的使用： 在工厂内部定义的方法中，使用 this 可以确保方法执行时指向的是当前被造出来的那个对象。
•	灵活性： 这种方式极大地提高了代码的可维护性。如果以后要给所有“人”对象增加一个“住址”属性，只需修改工厂函数这一处即可。
5. 工厂方法的局限性（重点）
虽然工厂方法很方便，但它有一个致命的缺陷，这也是后续引入“构造函数”的原因：
•	类型无法区分： 使用工厂方法创建的所有对象，其类型都是 Object。
•	无法识别身份： 你无法通过代码区分出“这个人对象”和“那个猫对象”有什么本质不同（因为它们都是由 new Object() 产生的）。在控制台中查看时，它们显示的都是统一的 Object 类型。
总结
•	工厂方法是封装思想的体现，解决了批量生产的问题。
•	核心公式： 创建对象 -> 装配属性 -> 返回对象。
•	不足： 缺乏“身份标识”，所有产出物在系统看来都是同一种“零件”。



JavaScript 中的构造函数（Constructor Function），它是面向对象编程中创建对象的核心方式。
1. 什么是构造函数？
构造函数本质上就是一个普通的函数，其定义方式与普通函数完全一样。
•	区别点： 它们唯一的区别在于调用方式。普通函数直接调用（如 fun()），而构造函数使用 new 关键字来调用（如 new Fun()）。
•	设计目的： 专门用来创建和初始化一个对象。
2. 构造函数的命名规范
为了在视觉上区分普通函数和构造函数，习惯上要求：
•	首字母大写（PascalCase）：例如 function Person(){...}，而普通函数通常首字母小写。
________________________________________
3. 使用 new 调用时的执行流程（核心重点）
当你使用 new 关键字调用一个函数时，解析器会按顺序执行以下四个隐含步骤：
1.	立刻创建一个新的对象。
2.	将 this 设置为这个新对象：这意味着在构造函数内部，this 就代表了当前正在创建的那个实例。
3.	逐行执行函数代码：通常是在函数内通过 this.属性名 = 值 的方式为新对象添加属性和方法。
4.	将新对象作为返回值返回：不需要手动写 return，系统会自动将造好的对象给到调用者。
________________________________________
4. 构造函数内部的 this
在构造函数中，使用 this 引用非常灵活：
•	它可以确保每个通过构造函数创建的对象都拥有独立的、属于自己的属性副本。
•	示例：
JavaScript
function Person(name, age) {
    this.name = name; // 将参数赋值给新对象的 name 属性
    this.age = age;
    this.sayName = function() {
        alert(this.name);
    };
}
________________________________________
5. instanceof 运算符
构造函数解决了一个大问题：对象的分类（类型识别）。
•	功能： 检查一个对象是否是一个构造函数的实例。
•	语法： 对象 instanceof 构造函数
•	返回值： 结果为 true 或 false。
•	意义： 比如你可以区分出 per 对象是 Person 类的实例，而 dog 对象是 Dog 类的实例。这在之前的“工厂方法”中是做不到的（工厂方法出来的全是 Object）。
•	所有对象都是 Object 的实例：任何对象执行 obj instanceof Object 都会返回 true。
________________________________________
6. 类（Class）的概念
•	通过构造函数创建的对象，我们称之为实例（Instance）。
•	构造函数本身就可以看作是一个类（Class）。它定义了一类对象的公共特征（属性和方法）。
•	在 JS 中，构造函数就是“类”的传统实现方式（在 ES6 之后引入了 class 关键字，但其底层依然是基于构造函数的机制）。
总结
•	构造函数是创建特定类型对象的蓝图。
•	new 关键字是触发构造逻辑的开关。
•	instanceof 是识别对象“身份”的工具。
•	相比工厂方法，构造函数让对象的分类更加清晰，代码结构更专业。




JavaScript 构造函数的性能优化，重点讨论了将方法定义在构造函数内部所带来的问题以及初步的解决方法。
 
1. 构造函数内部定义方法的问题
在之前的代码中，我们将方法（如 sayName）直接写在构造函数内部：
•	重复创建：每当使用 new 关键字创建一个新实例时，构造函数体内的代码都会执行一遍。这意味着，每创建一个对象，就会在内存中新创建一个功能完全一样的函数对象。
•	资源浪费：如果你创建了 10,000 个对象，内存中就会存在 10,000 个一模一样的函数。这会极大地占用内存空间，导致性能下降。
•	验证方法：通过比较两个实例的方法是否相等（如 per.sayName == per2.sayName），结果为 false。这证明了它们是不同的对象，尽管执行的逻辑完全一致。
2. 初步改进方案：全局定义
为了解决重复创建的问题，视频演示了一种优化方案：
•	核心思路：将函数定义在全局作用域中，然后在构造函数内部将该全局函数的引用赋值给对象的属性。
•	实现方式：
1.	在构造函数外部定义一个函数：function fun() { ... }。
2.	在构造函数内部：this.sayName = fun;。
•	优点：这样无论创建多少个对象，它们属性中保存的都是同一个函数的地址。内存中只存在一个该函数对象，从而节省了空间。
•	验证结果：此时执行 per.sayName == per2.sayName，结果将返回 true。
3. 全局定义方案的局限性
虽然上述方法解决了内存浪费问题，但它也带来了新的挑战：
•	污染全局命名空间：如果每个类都把方法写在全局作用域，会导致全局作用域非常混乱，容易发生命名冲突。
•	安全性与封装性差：这种写法破坏了对象的封装性，全局函数容易被误修改。
总结与展望
•	核心矛盾：构造函数内定义会浪费内存；全局定义会污染命名空间。
•	预告：视频最后指出，为了完美解决这两个矛盾，JavaScript 引入了**原型（Prototype）**机制。原型既能让所有实例共享方法（节省内存），又不需要将方法暴露在全局作用域中（保护命名空间）。




JavaScript 中非常核心且重要的概念：原型对象（Prototype）。原型机制解决了构造函数中“方法重复创建导致的内存浪费”以及“全局变量污染”这两大难题。
1. 原型（Prototype）的基本定义
•	函数的属性：我们创建的每一个函数，解析器都会向函数中添加一个属性 prototype。这个属性对应着一个对象，这个对象就是我们所谓的“原型对象”。
•	普通函数 vs 构造函数：
o	如果函数作为普通函数调用，prototype 没有任何作用。
o	当函数以构造函数的形式调用时，它所创建的每一个实例对象中都会有一个隐含的属性，指向该构造函数的原型对象。
2. 如何访问原型对象
•	构造函数访问：Person.prototype。
•	实例对象访问：我们可以通过 __proto__（前后各两个下划线）来访问实例对象的原型。
o	例如：per.__proto__ == Person.prototype 的结果为 true。
3. 原型的核心价值：共享与解耦
原型对象就相当于一个公共区域。
•	我们可以将所有实例中共有的内容（属性和方法），统一设置到原型对象中。
•	这样就不需要为每一个对象添加这些共有属性，也不会像“全局定义”那样污染全局作用域。
•	节省内存：无论创建多少个实例，原型对象里的方法在内存中只存在一份。
4. 属性和方法的搜索过程
当我们访问对象的一个属性或方法时，解析器的寻找逻辑如下：
1.	第一步：先在对象自身中寻找。如果有，则直接使用。
2.	第二步：如果自身没有，则去原型对象中寻找。如果有，则返回。
3.	第三步：如果原型中也没有，就会去原型的原型中找（直到找到 Object.prototype 为止，这构成了“原型链”）。
5. 实际应用：改写构造函数
在之前的例子中，我们为了性能将方法写在全局作用域。学习原型后，标准的做法应该是：
JavaScript
function Person(name, age) {
    this.name = name;
    this.age = age;
}

// 将方法添加到原型对象中，供所有实例共享
Person.prototype.sayName = function() {
    console.log("Hello, 我是" + this.name);
};

var per1 = new Person("孙悟空", 18);
var per2 = new Person("猪八戒", 28);

per1.sayName(); // 正常执行
console.log(per1.sayName == per2.sayName); // true，证明是同一个方法
总结
•	原型是 JS 对象的“共同祖先”。
•	它实现了数据的共享和内存的优化。
•	理解原型是掌握 JS 继承机制（Prototype Chain）的基础。




JavaScript **原型对象（Prototype）的进阶讲解，重点介绍了如何区分属性的归属以及原型链（Prototype Chain）**的完整结构。
 
1. 区分自身属性与原型属性
在 JS 中，由于原型机制的存在，一个对象可以访问到不属于它自身的属性（即定义在原型上的属性）。
•	in 运算符的局限性：使用 "属性名" in 对象 检查时，如果对象自身没有该属性但原型里有，它也会返回 true。这无法让我们确定属性到底是存在于对象实例中，还是存在于原型中。
•	hasOwnProperty() 方法：这是每个对象都拥有的方法，专门用来检查对象自身是否含有该属性。
o	语法：obj.hasOwnProperty("属性名")
o	返回值：只有当属性存在于对象自身（非原型）时，才返回 true。
________________________________________
2. 原型链（Prototype Chain）
这是 JavaScript 实现继承的核心机制。当我们访问一个对象的属性时，解析器会按照以下路径寻找：
1.	第一级（对象自身）：先看对象自己有没有这个属性。
2.	第二级（原型对象）：如果没有，则通过 __proto__ 找到其构造函数的原型对象。
3.	第三级（原型的原型）：如果原型对象里也没有，则会继续向上，寻找“原型的原型”。
4.	终点：原型链的最顶端是 Object.prototype。
________________________________________
3. 原型链的终点：Object.prototype
•	万物皆对象：在 JavaScript 中，几乎所有对象的原型链最终都会指向 Object.prototype。这也是为什么所有对象都能使用 toString()、hasOwnProperty() 等方法的原因，因为这些方法都定义在 Object.prototype 上。
•	终结符 null：Object 的原型的原型是 null（即 Object.prototype.__proto__ === null）。
•	搜索结果：如果在 Object.prototype 中依然找不到该属性，则返回 undefined。
________________________________________
4. 关键点总结
•	属性查找原则：采取“就近原则”，沿着原型链一级一级向上找，直到找到或到达 null。
•	方法的来源：我们通常把方法定义在原型对象中，目的是为了让所有实例共享同一份逻辑，节省内存。
•	hasOwnProperty 的位置：它本身也不在我们的自定义对象里，也不在自定义对象的原型里，而是在 Object.prototype 里。
通过理解原型链，你就能明白 JS 是如何通过这种链式结构实现属性共享和继承的，这是深入理解 JavaScript 的必经之路。
JavaScript 中的 toString() 方法。它是对象的一个核心方法，决定了对象在“字符串环境”下的表现形式。
1. toString() 的基本作用
当我们在页面中直接打印一个对象（如 console.log(obj) 或 alert(obj)）时，实际上是在调用该对象的 toString() 方法，将其转换为一个字符串并输出。
2. 默认的返回值
如果你没有对该方法进行任何处理，直接调用一个自定义对象的 toString()，它通常会返回： [object Object]
•	第一个 object 代表它是一个对象。
•	第二个 Object 代表该对象的类型（构造函数名）。 对于大多数开发者来说，这个默认返回值并没有太多实际意义，因为它无法告诉我们对象具体包含了哪些数据。
3. toString() 的来源
由于我们自己创建的对象里并没有写过这个方法，它之所以能被调用，是因为原型链机制：
•	它定义在 Object.prototype 中。
•	所有的 JS 对象（通过原型链）最终都会指向 Object.prototype，因此它们都继承了这个方法。
4. 如何修改（重写）toString()
为了让对象打印出来时显示更直观的信息（例如打印出：Person[name=孙悟空, age=18]），我们需要重写这个方法。
① 为单个对象重写（不推荐）
你可以直接给某个具体的对象实例添加一个 toString 属性。
JavaScript
per.toString = function() {
    return "Person[name=" + this.name + ", age=" + this.age + "]";
};
•	缺点：这种方式只对这一个对象有效，如果再创建一个新对象，它还是会显示默认值。
② 在原型中重写（标准做法）
通过修改构造函数的原型对象，可以让该类所有的实例都共享这个新的展示逻辑。
JavaScript
function Person(name, age) {
    this.name = name;
    this.age = age;
}

// 修改原型的 toString 方法
Person.prototype.toString = function() {
    return "Person[name=" + this.name + ", age=" + this.age + "]";
};

var per1 = new Person("孙悟空", 18);
var per2 = new Person("猪八戒", 28);

console.log(per1.toString()); // 输出自定义内容
console.log(per2.toString()); // 同样输出自定义内容
5. 查找优先级
当你调用 per.toString() 时：
1.	首先在 per 对象自身寻找。
2.	如果没有，去 Person.prototype（我们重写的地方）寻找。
3.	如果还没重写，才会去 Object.prototype 寻找默认的实现。
总结
•	toString() 是对象自我介绍的“名片”。
•	默认名片很简陋（[object Object]），不具备辨识度。
•	重写名片通常在原型对象上进行，目的是为了在调试和日志记录时，能清晰地看到对象的内部状态。



JavaScript 中的**垃圾回收（Garbage Collection，简称 GC）**机制。在编写程序时，无论是变量、函数还是对象，都会占用内存空间。如果这些不再需要的数据一直堆积，会导致内存溢出，使程序运行变得极其缓慢甚至崩溃。
 
1. 什么是垃圾（Garbage）？
在 JavaScript 中，当一个对象没有任何变量或属性对它进行引用时，这个对象就会变成“垃圾”。
•	现象：该对象依然占据着堆内存空间，但程序已经无法通过任何路径访问到它。
•	后果：这种对象过大会导致程序性能下降，必须将其清理。
2. 垃圾回收机制
JavaScript 拥有一套自动的垃圾回收机制，开发者不需要（也无法）手动去编写代码来销毁对象（这与 C++ 等需要手动释放内存的语言不同）。
•	原理：浏览器（JS 引擎）会自动运行垃圾回收器。它会定期寻找并识别出内存中不再被引用的对象，然后将其所占用的内存空间释放，供后续使用。
•	自动性：我们不能强制开启或关闭垃圾回收，它是由系统根据性能需求自动运行的。
3. 核心算法：标记清除（Mark-and-Sweep）
虽然垃圾回收是自动的，但了解其底层逻辑有助于编写更高质量的代码。主流浏览器主要采用“标记清除”算法：
1.	标记阶段：垃圾回收器从“根”（通常是全局变量 window）开始遍历，标记所有能被访问到的对象（即可达对象）。
2.	清除阶段：垃圾回收器会扫描内存，将那些没有被标记的对象（即不可达对象）视为垃圾并进行回收。
4. 解决循环引用问题（对比引用计数）
早期的浏览器（如 IE6/7）使用“引用计数”算法，如果两个对象互相引用，它们的引用次数永远不会归零，从而导致内存泄漏。而现代的“标记清除”算法完美解决了这个问题：如果这一组互相引用的对象从根节点开始无法到达，它们依然会被回收。
5. 开发者如何配合垃圾回收？
虽然我们不能操作回收器，但我们可以主动创造回收条件。
•	知识点：置空（Nullify）：如果你确定一个全局变量指向的大对象不再使用了，应该手动将其赋值为 null。
•	原理：将变量赋值为 null 会断开该变量与对象之间的引用联系。如果该对象没有了其他引用，它在下一次垃圾回收运行时就会被自动清理。
JavaScript
var obj = new Object();
// ... 使用完毕后
obj = null; // 此时对象变为垃圾，等待回收
总结
•	程序性能：垃圾回收是保证 JS 程序流畅运行的基础。
•	堆内存管理：它主要负责清理堆内存（Heap）中的对象。
•	最佳实践：不再需要的全局对象、大型数据结构，养成手动置为 null 的习惯，是优化内存占用、预防内存泄漏的高效手段。


JavaScript 中**数组（Array）**的基础知识。数组是 JS 中处理有序数据最常用的工具。
、
1. 数组的基本概念
•	本质：数组也是一个对象（Object）。它和普通对象的功能类似，都是用来存储值的。
•	区别：
o	普通对象：使用字符串作为属性名（Key）来访问数据。
o	数组：使用从 0 开始的数字作为**索引（Index）**来管理元素。
•	性能：数组存储有序数据的效率比普通对象更高，因此在开发中我们通常使用数组来存储一组相关联的数据。
2. 基本操作
•	创建数组：使用构造函数创建，语法为 var arr = new Array();。
•	向数组添加元素：
o	语法：数组[索引] = 值;。
o	例如：arr[0] = 10; arr[1] = 33;。
•	读取元素：
o	语法：数组[索引]。
o	如果读取不存在的索引（即没有赋值的位置），会返回 undefined 而不会报错。
3. length 属性详解
length 是数组中最重要的属性，它表示数组的长度。
•	读取长度：
o	对于连续的数组，length 获取的就是元素的个数。
o	连续数组与非连续数组：如果在索引 0 赋值后，直接在索引 100 赋值，length 会返回 101（最大索引 + 1）。这种中间有空缺的数组称为“稀疏数组”，开发中应尽量避免使用。
•	修改长度：
o	改大：如果将 length 设置为一个比原长度大的值，数组中会多出空的区域（值为 undefined）。
o	改小：如果将 length 设置为一个比原长度小的值，数组多出的元素会被永久删除。这是截断数组的一种常用手段。
4. 开发技巧
•	动态向末尾添加元素：利用 length 总是等于“最大索引 + 1”的特性，可以使用以下语法永远向数组的最后一个位置添加新数据： arr[arr.length] = 值; 
总结
数组是专门设计用来存储有序数据的对象，通过数字索引操作元素。理解 length 属性与索引之间的对应关系是掌握数组后续高级方法（如遍历、增删改查）的基础。

